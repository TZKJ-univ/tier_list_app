name: test

on:
  workflow_run:
    workflows: ["Auto Create PR on new push"]
    types:
      - completed
  push:
    branches:
      - '**'
      - '!main'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run Tests
        run: |
          echo "Running tests in test.yml workflow"